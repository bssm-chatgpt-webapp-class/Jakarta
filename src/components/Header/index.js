import React from "react";
import { PlusIcon, HambugIcon } from "../../icons";

const Header = () => {
  return (
    <div>
      <PlusIcon />
      <div>New Chat</div>
      <HambugIcon />
    </div>
  );
};

export default Header;
